class Attendance {

    constructor(kurs, video, datum,start) {
      this.kurs = kurs;
      this.video = video;
      this.datum = datum;
      this.start = start;
    }
  }
  
  export default Attendance;